RSpec.describe "my first method" do
  it 'does something really cool' do
    expect(my_first_method(32)).to eq 0
    expect(my_first_method(212)).to eq 100
  end
end

RSpec.describe "my second method" do
  it 'does something really cool' do
    expect(my_second_method(32)).to eq 0
    expect(my_second_method(212)).to eq 100
  end
end